import { SongNameDirective } from './song-name.directive';

describe('SongNameDirective', () => {
  it('should create an instance', () => {
    const directive = new SongNameDirective();
    expect(directive).toBeTruthy();
  });
});
